@model books.Models.AdminViewModels.DillerVM
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Dil Sil</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/Admin">Anasayfa</a></li>
                    <li class="breadcrumb-item"><a href="/Admin/Diller">Diller</a></li>
                    <li class="breadcrumb-item active">Dil Sil</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-6">
                <div class="card card-danger">
                    <div class="alert alert-warning fw-bolder text-center" role="alert">
                        Bu dili silmek istediğinize emin misiniz?
                    </div>
                    <form class="form-horizontal" method="post" asp-controller="Admin" asp-action="DilSilmeOnay" asp-route-id="@ViewBag.DilInfo.Id">
                        <input type="hidden" asp-for="Id" value="@ViewBag.DilInfo.Id" />
                        @Html.AntiForgeryToken()
                        <div class="card-body">
                            <div class="form-group">
                                <label>Dil Adı</label>
                                <input type="text" class="form-control" disabled value="@ViewBag.DilInfo.Adi">
                            </div>
                            <div class="form-group">
                                <label>Dil Kodu</label>
                                <input type="text" class="form-control" disabled value="@ViewBag.DilInfo.Kod">
                            </div>
                        </div>
                        <div class="card-footer">
                            <a href="/Admin/Diller" class="btn btn-default">Vazgeç</a>
                            <button type="submit" class="btn btn-danger float-right">Sil</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> 