<nav class="navbar navbar-expand-lg">
    <div class="container">
        <a class="navbar-brand" href="/">Kitap Blog</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/">Anasayfa</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Kitaplar">Kitaplar</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/Home/Iletisim">İletişim</a>
                </li>
            </ul>
            <div class="d-flex align-items-center">
                <button class="theme-toggle me-3" onclick="toggleTheme()">
                    <i class="fas fa-moon" id="theme-icon"></i>
                </button>
                @if (User.Identity.IsAuthenticated)
                {
                    var user = Context.RequestServices.GetService<books.Models.KitapDbContext>()
                        .Kullanicilars.FirstOrDefault(x => x.usernames == User.Identity.Name);
                    <div class="dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                            <img src="~/images/users/@(user?.resim ?? "default.jpg")" class="rounded-circle me-2" 
                                 style="width: 32px; height: 32px; object-fit: cover;" 
                                 alt="Profil">
                            <span>@user?.isim @user?.soyisim</span>
                        </a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="/Account/Profil">Profilim</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="/Account/Logout">Çıkış Yap</a>
                        </div>
                    </div>
                }
                else
                {
                    <a href="/Account/Giris" class="btn btn-outline-primary me-2">Giriş Yap</a>
                    <a href="/Account/KayitOl" class="btn btn-outline-success">Kaydol</a>
                }
            </div>
        </div>
    </div>
</nav>